@inherits WindowBase

<TelerikWindow Visible="@Visible" Top="@Top" Left="@Left" Height="@Height" Width="@Width" LeftChanged="@(s => WindowResized(WindowAttribute.Left, $"{s}"))" HeightChanged="@(s => WindowResized(WindowAttribute.Height, $"{s}"))" TopChanged="@(s => WindowResized(WindowAttribute.Top, $"{s}"))" WidthChanged="@(s => WindowResized(WindowAttribute.Width, $"{s}"))">
  <WindowActions>
    <WindowAction Name="Close" OnClick="@Close" />
  </WindowActions>
  <WindowTitle>Donors</WindowTitle>
  <WindowContent>
    <TelerikGrid Data="Donors"
                 Class="border-0">
      <GridColumns>
        <GridColumn Field="@nameof(Customer.Id)" Width="70px" />
        <GridColumn Field="@nameof(Customer.Name)" />
        <GridColumn Field="@nameof(Customer.Address)" />
      </GridColumns>
    </TelerikGrid>
  </WindowContent>
</TelerikWindow>

@code {

  private List<Customer> Donors { get; } = DataService.Customers.Take(20).ToList();

}